import N8NDashboard from './components/N8NDashboard';
import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';

function App() {
  return (
    <Router>
      <Routes>
        <Route path="/dashboard" element={<N8NDashboard />} />
        {/* Your other routes */}
      </Routes>
    </Router>
  );
}

export default App;